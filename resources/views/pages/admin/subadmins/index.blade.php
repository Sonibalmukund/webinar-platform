@extends('layouts.portal')
@section('title','Sub Admins')
@section('content')
<div class="page-heading"><div><span class="eyebrow">ADMIN WORKSPACE</span><h1>Sub admins</h1><p>Manage staff and their assigned webinars.</p></div><a class="btn btn-gradient" href="{{ route('admin.subadmins.create') }}"><i class="bi bi-plus"></i> Add sub admin</a></div>@if(session('status'))<div class="alert alert-success">{{ session('status') }}</div>@endif
<div class="panel-card table-responsive"><table class="premium-table"><thead><tr><th>Name</th><th>Email</th><th>Assigned webinars</th><th>Status</th><th>Actions</th></tr></thead><tbody>@forelse($subAdmins as $user)<tr><td><strong>{{ $user->name }}</strong><br><small>{{ $user->job_title }}</small></td><td>{{ $user->email }}</td><td>@forelse($user->assignedWebinars as $webinar)<span class="status-badge scheduled me-1">{{ $webinar->title }}</span>@empty<span class="text-muted">No webinar assigned</span>@endforelse</td><td><span class="status-badge {{ $user->status==='active'?'completed':'draft' }}">{{ ucfirst($user->status) }}</span></td><td><div class="d-flex gap-1"><a class="btn btn-sm btn-light" href="{{ route('admin.subadmins.edit',$user) }}">Edit</a><form method="POST" action="{{ route('admin.subadmins.destroy',$user) }}" onsubmit="return confirm('Delete this sub admin?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Delete</button></form></div></td></tr>@empty<tr><td colspan="5" class="text-center py-5 text-muted">No sub admins added.</td></tr>@endforelse</tbody></table></div>{{ $subAdmins->links() }}
@endsection
