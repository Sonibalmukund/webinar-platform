@extends('layouts.portal')
@section('title','Certificate Queue')
@section('content')
<div class="page-heading"><div><span class="eyebrow">ELIGIBILITY WORKFLOW</span><h1>Certificate approval queue</h1><p>Eligible attendees appear automatically based on attendance and poll rules.</p></div><a class="btn btn-light" href="{{ route('admin.certificates.index') }}">Certificate templates</a></div>@if(session('status'))<div class="alert alert-success">{{ session('status') }}</div>@endif
<div class="panel-card table-responsive"><table class="premium-table"><thead><tr><th>Index</th><th>Attendee</th><th>Webinar</th><th>Credential</th><th>Status</th><th>Decision</th></tr></thead><tbody>@forelse($rows as $row)<tr><td>{{ $rows->firstItem()+$loop->index }}</td><td><strong>{{ $row->user_name }}</strong><br><small>{{ $row->email }}</small></td><td>{{ $row->webinar_title }}</td><td><code>{{ Str::limit($row->credential_id,12) }}</code></td><td><span class="status-badge {{ $row->status==='approved'?'active':($row->status==='rejected'?'cancelled':'draft') }}">{{ ucfirst($row->status) }}</span></td><td>@if($row->status==='pending')<div class="d-flex gap-1"><form method="POST" action="{{ route('admin.certificates.decision',$row->id) }}">@csrf @method('PATCH')<input type="hidden" name="status" value="approved"><button class="btn btn-sm btn-success">Approve</button></form><form method="POST" action="{{ route('admin.certificates.decision',$row->id) }}">@csrf @method('PATCH')<input type="hidden" name="status" value="rejected"><button class="btn btn-sm btn-outline-danger">Reject</button></form></div>@else<small>{{ $row->reviewed_at?Carbon\Carbon::parse($row->reviewed_at)->format('d M Y'):'Reviewed' }}</small>@endif</td></tr>@empty<tr><td colspan="6" class="text-center py-5 text-muted">No eligible attendees are waiting for review.</td></tr>@endforelse</tbody></table></div><div class="mt-3">{{ $rows->links() }}</div>
@endsection
